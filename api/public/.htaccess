Options -Indexes
RewriteEngine On

# Serve real files directly; send every API route to the front controller.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]

import { useQuery } from '@tanstack/react-query';
import { api } from '../../services/api';
import type { AdminUser } from '../../types/admin';
export function AdminUsersPage() {
  const query = useQuery({ queryKey: ['admin-users'], queryFn: async () => (await api.get<{ data: AdminUser[] }>('/admin/users')).data.data });
  return <div><div><p className="text-sm font-semibold text-brand-700">Identity</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Users</h1><p className="mt-2 text-muted">Platform and customer identities across WhatstheUp.</p></div><div className="mt-8 overflow-hidden rounded-2xl border border-line bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-gray-50 text-xs uppercase tracking-wide text-muted"><tr><th className="px-5 py-3">User</th><th className="px-5 py-3">Workspace</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Verified</th><th className="px-5 py-3">Last login</th></tr></thead><tbody className="divide-y divide-line">{query.data?.map((user) => <tr key={user.id}><td className="px-5 py-4"><p className="font-medium">{user.name}</p><p className="text-muted">{user.email}</p></td><td className="px-5 py-4">{user.workspaces}</td><td className="px-5 py-4"><Status value={user.status}/></td><td className="px-5 py-4">{user.emailVerified ? 'Yes' : 'No'}</td><td className="px-5 py-4 text-muted">{user.lastLoginAt ?? 'Never'}</td></tr>)}</tbody></table></div>{query.isLoading && <p className="p-8 text-center text-muted">Loading users…</p>}{!query.isLoading && query.data?.length === 0 && <p className="p-8 text-center text-muted">No users found.</p>}</div></div>;
}
function Status({ value }: { value: string }) { return <span className="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-medium capitalize text-brand-700">{value}</span>; }
